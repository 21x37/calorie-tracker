
const followersReducerDefaultState = [];

const followersReducer = (state = followersReducerDefaultState, action) => {
    switch (action.type) {
        default:
            return state;
    };
};

export default followersReducer;